export class RecordList {
  headerRow?: string[];
  dataRows?: DataRows[];
}
export class DataRows {
  examId?: string;
  examTeacher?: string;
  examName?: string;
  examTime?: string;
  examLog?: string;
  studentId?: string;
}
